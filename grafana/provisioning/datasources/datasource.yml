apiVersion: 1

datasources:
  - name: Governance Postgres
    type: postgres
    access: proxy
    orgId: 1
    isDefault: true
    editable: true

    host: postgres
    port: 5432
    user: postgres
    database: governance
    sslmode: disable

    jsonData:
      sslmode: disable
      postgresVersion: 1600

    secureJsonData:
      password: admin